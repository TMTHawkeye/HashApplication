package hashtag.generator.hashapplication.Saved

import co.lujun.androidtagview.Tagmodel

interface savedFilesInterface {
    fun show_card_Selected(list_of_file:ArrayList<Tagmodel>)
     fun showNoItem(b: Boolean)
}